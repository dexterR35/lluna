import { Component } from "react"; import { Button } from "./Button"; export class ErrorBoundary extends Component{state={error:null};static getDerivedStateFromError(error){return{error}}componentDidCatch(error,info){console.error("Renderer failure",error,info)}render(){if(this.state.error)return <div role="alert" className="grid h-screen place-items-center bg-mg-app p-8"><div className="max-w-lg rounded-xl border border-mg-error bg-mg-panel p-6"><h1 className="text-lg font-semibold text-mg-primary">The editor hit an unexpected error</h1><p className="mt-2 text-sm text-mg-secondary">{this.state.error.message}</p><Button className="mt-5" onClick={()=>location.reload()}>Restart renderer</Button></div></div>;return this.props.children}}
