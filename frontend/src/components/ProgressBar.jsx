export function ProgressBar({value=0,label="Progress",showLabel=false}){const clamped=Math.max(0,Math.min(100,value));return <div className="grid gap-1"><div role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={clamped} className="h-1.5 overflow-hidden rounded-full bg-mg-app"><div className="h-full rounded-full bg-mg-running transition-[width]" style={{width:`${clamped}%`}}/></div>{showLabel&&<span className="text-xs text-mg-secondary">{label} · {Math.round(clamped)}%</span>}</div>}
