export function CircularProgress({value=0,label="Progress",size=28}){const radius=10,circ=2*Math.PI*radius,clamped=Math.max(0,Math.min(100,value));return <svg role="progressbar" aria-label={label} aria-valuenow={clamped} viewBox="0 0 24 24" width={size} height={size} className="-rotate-90"><circle cx="12" cy="12" r={radius} fill="none" stroke="var(--mg-border)" strokeWidth="2"/><circle cx="12" cy="12" r={radius} fill="none" stroke="var(--mg-running)" strokeWidth="2" strokeDasharray={circ} strokeDashoffset={circ*(1-clamped/100)} strokeLinecap="round"/></svg>}
