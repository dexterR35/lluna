import { forwardRef } from "react"; import { cn,focusRing } from "./utils"; export const Select=forwardRef(function Select({label,options=[],error,className="",...props},ref){return <label className="grid gap-1.5 text-sm text-mg-secondary">{label&&<span>{label}</span>}<select ref={ref} aria-invalid={Boolean(error)} className={cn("h-9 rounded-md border border-mg-border bg-mg-app px-3 text-mg-primary",focusRing,error&&"border-mg-error",className)} {...props}>{options.map(option=><option key={String(option.value)} value={option.value} disabled={option.disabled}>{option.label}</option>)}</select>{error&&<span role="alert" className="text-xs text-mg-error">{error}</span>}</label>});
