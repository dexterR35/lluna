import { forwardRef } from "react"; import { cn } from "./utils"; export const Select=forwardRef(function Select({label,options=[],error,hint,className="",...props},ref){return <label className="ui-field-label">{label&&<span>{label}</span>}<select ref={ref} aria-invalid={Boolean(error)} className={cn("ui-control",error&&"border-mg-error",className)} {...props}>{options.map(option=><option key={String(option.value)} value={option.value} disabled={option.disabled}>{option.label}</option>)}</select>{error&&<span role="alert" className="ui-help text-mg-error">{error}</span>}{!error&&hint&&<span className="ui-help">{hint}</span>}</label>});
