import { Button } from "./Button"; import { Dialog } from "./Dialog";
/** @param {{open: boolean, onClose: () => void, onConfirm: () => void, title?: string, message?: import("react").ReactNode, confirmLabel?: string, danger?: boolean}} props */
export function ConfirmDialog({open,onClose,onConfirm,title="Confirm action",message,confirmLabel="Confirm",danger=false}){return <Dialog open={open} onClose={onClose} title={title} footer={<><Button variant="ghost" onClick={onClose}>Cancel</Button><Button variant={danger?"danger":"primary"} onClick={()=>{onConfirm();onClose();}}>{confirmLabel}</Button></>}>{message&&<p className="text-sm leading-6 text-mg-secondary">{message}</p>}</Dialog>}
