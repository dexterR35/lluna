import { forwardRef } from "react"; import { cn,focusRing } from "./utils"; export const TextField=forwardRef(function TextField({label,error,hint,className="",...props},ref){const id=props.id||`field-${String(label).toLowerCase().replaceAll(" ","-")}`;return <label className="grid gap-1.5 text-sm text-mg-secondary" htmlFor={id}>{label&&<span>{label}</span>}<input ref={ref} id={id} aria-invalid={Boolean(error)} aria-describedby={error?`${id}-error`:undefined} className={cn("h-9 rounded-md border border-mg-border bg-mg-app px-3 text-mg-primary placeholder:text-mg-secondary/60 disabled:opacity-50",focusRing,error&&"border-mg-error",className)} {...props}/>{error&&<span id={`${id}-error`} role="alert" className="text-xs text-mg-error">{error}</span>}{!error&&hint&&<span className="text-xs">{hint}</span>}</label>});
