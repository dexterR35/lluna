import { forwardRef } from "react"; import { cn } from "./utils"; export const TextField=forwardRef(function TextField({label,error,hint,className="",...props},ref){const id=props.id||`field-${String(label||"input").toLowerCase().replaceAll(" ","-")}`;return <label className="ui-field-label" htmlFor={id}>{label&&<span>{label}</span>}<input ref={ref} id={id} aria-invalid={Boolean(error)} aria-describedby={error?`${id}-error`:undefined} className={cn("ui-control",error&&"border-mg-error",className)} {...props}/>{error&&<span id={`${id}-error`} role="alert" className="ui-help text-mg-error">{error}</span>}{!error&&hint&&<span className="ui-help">{hint}</span>}</label>});
