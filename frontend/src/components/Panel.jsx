import { cn } from "./utils"; export function Panel({title,actions,children,className="",bodyClassName=""}){return <section className={cn("flex min-h-0 flex-col border-mg-border bg-mg-panel",className)}>{(title||actions)&&<header className="flex min-h-11 items-center justify-between border-b border-mg-border px-3"><h2 className="text-xs font-semibold uppercase tracking-wider text-mg-secondary">{title}</h2>{actions}</header>}<div className={cn("min-h-0 flex-1",bodyClassName)}>{children}</div></section>}
