import { Search,X } from "lucide-react"; import { IconButton } from "./IconButton"; import { cn,focusRing } from "./utils"; export function SearchInput({value,onChange,placeholder="Search…",label="Search",autoFocus=false}){return <label className={cn("flex h-9 items-center gap-2 rounded-md border border-mg-border bg-mg-app px-2",focusRing)}><Search aria-hidden className="size-4 text-mg-secondary"/><span className="sr-only">{label}</span><input autoFocus={autoFocus} value={value} onChange={event=>onChange(event.target.value)} placeholder={placeholder} className="min-w-0 flex-1 bg-transparent text-sm text-mg-primary outline-none placeholder:text-mg-secondary/60"/>{value&&<IconButton label="Clear search" className="size-6" onClick={()=>onChange("")}><X className="size-3.5"/></IconButton>}</label>}
