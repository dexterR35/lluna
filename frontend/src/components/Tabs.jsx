import { cn,focusRing } from "./utils"; export function Tabs({tabs,value,onChange,label="Tabs",className=""}){function key(event,index){let next=index;if(event.key==="ArrowRight")next=(index+1)%tabs.length;else if(event.key==="ArrowLeft")next=(index-1+tabs.length)%tabs.length;else return;event.preventDefault();onChange(tabs[next].id);event.currentTarget.parentElement.children[next].focus();}return <div role="tablist" aria-label={label} className={cn("flex h-9 items-center gap-1 border-b border-mg-border px-2",className)}>{tabs.map((tab,index)=><button key={tab.id} role="tab" aria-selected={value===tab.id} tabIndex={value===tab.id?0:-1} onKeyDown={event=>key(event,index)} onClick={()=>onChange(tab.id)} className={cn("inline-flex h-6 items-center gap-1.5 rounded-md px-2 text-[10px] font-medium transition",focusRing,value===tab.id?"bg-mg-selected text-mg-primary":"text-mg-muted hover:bg-mg-elevated hover:text-mg-secondary")}>{tab.icon}{tab.label}{tab.count!==undefined&&<span className="text-[9px] opacity-60">{tab.count}</span>}</button>)}</div>}
