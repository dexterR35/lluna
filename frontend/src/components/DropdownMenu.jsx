import { cn,focusRing } from "./utils"; export function DropdownMenu({items,onSelect}){return <div role="menu" className="grid min-w-52 gap-1 p-1">{items.map((entry,index)=>entry.separator?<hr key={index} className="my-1 border-mg-border"/>:<button key={entry.id} role="menuitem" disabled={entry.disabled} onClick={()=>onSelect(entry.id)} className={cn("flex min-h-9 items-center justify-between rounded px-3 text-left text-sm text-mg-primary hover:bg-mg-selected disabled:opacity-40",focusRing)}><span>{entry.label}</span>{entry.shortcut&&<kbd className="text-xs text-mg-secondary">{entry.shortcut}</kbd>}</button>)}</div>}
