import { Button } from "./Button"; export function ToolbarButton({label,icon,shortcut,active=false,...props}){return <Button variant="ghost" aria-pressed={active||undefined} title={shortcut?`${label} (${shortcut})`:label} className={active?"bg-mg-selected text-mg-primary":""} {...props}>{icon}<span className="hidden 2xl:inline">{label}</span></Button>}
