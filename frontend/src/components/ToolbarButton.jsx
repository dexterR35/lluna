import { Button } from "./Button"; export function ToolbarButton({label,icon,shortcut,active=false,showLabel=false,...props}){return <Button variant="ghost" aria-pressed={active||undefined} title={shortcut?`${label} (${shortcut})`:label} className={`min-w-8 px-2 ${active?"border-mg-border bg-mg-selected text-mg-primary":""}`} {...props}>{icon}{showLabel&&<span>{label}</span>}</Button>}
