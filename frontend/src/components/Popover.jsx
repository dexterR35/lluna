import { useEffect,useRef } from "react"; export function Popover({open,onClose,anchor,children,align="left"}){const ref=useRef(null);useEffect(()=>{if(!open)return;const close=event=>{if(!ref.current?.contains(event.target))onClose();};document.addEventListener("pointerdown",close);return()=>document.removeEventListener("pointerdown",close);},[open,onClose]);return <span ref={ref} className="relative inline-flex">{anchor}{open&&<span role="dialog" className={`absolute top-full z-40 mt-2 min-w-56 rounded-lg border border-mg-border bg-mg-panel p-2 shadow-panel ${align==="right"?"right-0":"left-0"}`}>{children}</span>}</span>}
