import { ChevronRight } from "lucide-react"; import { cn,focusRing } from "./utils";
/** @typedef {{id: string, label: import("react").ReactNode, children?: TreeItem[]}} TreeItem */
/** @param {{items: TreeItem[], expanded: string[], onToggle: (id: string) => void, onSelect: (id: string) => void, selected?: string}} props */
export function TreeView({items,expanded,onToggle,onSelect,selected}){return <ul role="tree" className="grid gap-0.5">{items.map(item=><li key={item.id} role="treeitem" aria-expanded={item.children?expanded.includes(item.id):undefined}><button type="button" onClick={()=>item.children?onToggle(item.id):onSelect(item.id)} className={cn("flex min-h-8 w-full items-center gap-1.5 rounded-xl px-2.5 text-left text-[12px]",focusRing,selected===item.id?"bg-mg-elevated text-mg-primary":"text-mg-secondary hover:bg-mg-app hover:text-mg-primary")}><ChevronRight className={cn("size-3.5",!item.children&&"invisible",expanded.includes(item.id)&&"rotate-90")}/>{item.label}</button>{item.children&&expanded.includes(item.id)&&<div className="ml-4"><TreeView items={item.children} expanded={expanded} onToggle={onToggle} onSelect={onSelect} selected={selected}/></div>}</li>)}</ul>}
