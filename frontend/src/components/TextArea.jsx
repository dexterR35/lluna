import { forwardRef } from "react"; import { cn } from "./utils"; export const TextArea=forwardRef(function TextArea({label,error,hint,className="",...props},ref){return <label className="ui-field-label">{label&&<span>{label}</span>}<textarea ref={ref} aria-invalid={Boolean(error)} className={cn("ui-control min-h-20 resize-y py-2",error&&"border-mg-error",className)} {...props}/>{error&&<span role="alert" className="ui-help text-mg-error">{error}</span>}{!error&&hint&&<span className="ui-help">{hint}</span>}</label>});
