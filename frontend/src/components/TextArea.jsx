import { forwardRef } from "react"; import { cn,focusRing } from "./utils"; export const TextArea=forwardRef(function TextArea({label,error,className="",...props},ref){return <label className="grid gap-1.5 text-sm text-mg-secondary">{label&&<span>{label}</span>}<textarea ref={ref} aria-invalid={Boolean(error)} className={cn("min-h-24 resize-y rounded-md border border-mg-border bg-mg-app p-3 text-mg-primary",focusRing,error&&"border-mg-error",className)} {...props}/>{error&&<span role="alert" className="text-xs text-mg-error">{error}</span>}</label>});
