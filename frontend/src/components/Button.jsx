import { forwardRef } from "react"; import { LoaderCircle } from "lucide-react"; import { cn, focusRing } from "./utils";
export const Button = forwardRef(function Button({ children, variant="primary", loading=false, className="", disabled=false, ...props }, ref) { const variants={primary:"border border-mg-accent bg-mg-accent text-white hover:brightness-110",secondary:"border border-mg-border bg-mg-elevated text-mg-primary hover:border-mg-secondary/40 hover:bg-mg-selected",danger:"border border-mg-error/70 bg-mg-error/10 text-mg-error hover:bg-mg-error hover:text-white",ghost:"border border-transparent text-mg-secondary hover:bg-mg-selected hover:text-mg-primary"}; return <button ref={ref} type="button" disabled={disabled||loading} aria-busy={loading||undefined} className={cn("inline-flex min-h-8 items-center justify-center gap-1.5 rounded-lg px-2.5 text-xs font-semibold transition disabled:cursor-not-allowed disabled:opacity-40",focusRing,variants[variant],className)} {...props}>{loading&&<LoaderCircle aria-hidden className="size-3.5 animate-spin"/>}{children}</button>; });
