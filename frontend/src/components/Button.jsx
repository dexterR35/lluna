import { forwardRef } from "react"; import { LoaderCircle } from "lucide-react"; import { cn, focusRing } from "./utils";
export const Button = forwardRef(function Button({ children, variant="primary", loading=false, className="", disabled=false, ...props }, ref) { const variants={primary:"bg-mg-accent text-mg-app hover:brightness-110",secondary:"border border-mg-border bg-mg-panel text-mg-primary hover:bg-mg-selected",danger:"bg-mg-error text-mg-app hover:brightness-110",ghost:"text-mg-secondary hover:bg-mg-selected hover:text-mg-primary"}; return <button ref={ref} type="button" disabled={disabled||loading} aria-busy={loading||undefined} className={cn("inline-flex min-h-9 items-center justify-center gap-2 rounded-md px-3 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-45",focusRing,variants[variant],className)} {...props}>{loading&&<LoaderCircle aria-hidden className="size-4 animate-spin"/>}{children}</button>; });
